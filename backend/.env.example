# ─── Environment ─────────────────────────────────────────────────────
# Set to "production" for production deploys.  Enables HSTS, blocks
# default JWT secrets, and other hardening.
ENVIRONMENT=development

# ─── Database ────────────────────────────────────────────────────────
# Async PostgreSQL connection string.  Use "db" as host when running
# via Docker Compose; use "localhost" when running Postgres locally.
DATABASE_URL=postgresql+asyncpg://courier:courier@db:5432/courier

# ─── JWT Authentication ──────────────────────────────────────────────
# Generate with: python3 -c "import secrets; print(secrets.token_urlsafe(32))"
# MUST be changed from defaults before deploying to production.
JWT_SECRET_KEY=change-me-in-production
JWT_REFRESH_SECRET_KEY=change-me-to-another-random-secret

# ─── First Admin (seed) ─────────────────────────────────────────────
FIRST_ADMIN_EMAIL=admin@example.com
FIRST_ADMIN_PASSWORD=changeme

# ─── Stripe (optional) ──────────────────────────────────────────────
# Leave blank for stub/dev mode (payments are mocked).
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=

# ─── CORS ────────────────────────────────────────────────────────────
# Comma-separated allowed origins.  In production, set to your actual
# frontend domain(s).  Example:
#   CORS_ORIGINS=https://app.pedaloverpetrol.com,https://portal.pedaloverpetrol.com
CORS_ORIGINS=http://localhost:3000

# ─── API Documentation ──────────────────────────────────────────────
# Set to false in production to hide Swagger UI / OpenAPI schema.
ENABLE_DOCS=true

# ─── Logging ─────────────────────────────────────────────────────────
LOG_LEVEL=INFO
